<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.github.hualuomoli.extend.base.mapper.BaseProductMapper">

  <sql id="columns">
    `id`          as "id"         ,
    `name`        as "name"       ,
    `description` as "description",
    `price`       as "price"      ,
    `price_str`   as "priceStr"   ,
    `amount`      as "amount"     ,
    `create_by`   as "createBy"   ,
    `create_date` as "createDate" ,
    `update_by`   as "updateBy"   ,
    `update_date` as "updateDate" ,
    `status`      as "status"     ,
    `status_name` as "statusName" 
  </sql>
  
  <sql id="querys">
    <if test="id != null and id != ''"> 
      and `id`          = #{id}         
    </if>
    <if test="name != null and name != ''"> 
      and `name`        = #{name}       
    </if>
    <if test="description != null and description != ''"> 
      and `description` = #{description}
    </if>
    <if test="price != null"> 
      and `price`       = #{price}      
    </if>
    <if test="priceStr != null"> 
      and `price_str`   = #{priceStr}   
    </if>
    <if test="amount != null"> 
      and `amount`      = #{amount}     
    </if>
    <if test="createBy != null and createBy != ''"> 
      and `create_by`   = #{createBy}   
    </if>
    <if test="createDate != null"> 
      and `create_date` = #{createDate} 
    </if>
    <if test="updateBy != null and updateBy != ''"> 
      and `update_by`   = #{updateBy}   
    </if>
    <if test="updateDate != null"> 
      and `update_date` = #{updateDate} 
    </if>
    <if test="status != null"> 
      and `status`      = #{status}     
    </if>
    <if test="statusName != null and statusName != ''"> 
      and `status_name` = #{statusName} 
    </if>
	<!-- 比较查询 -->
  </sql>
  
  <select id="get" resultType="com.github.hualuomoli.extend.base.entity.BaseProduct">
    select 
      <include refid="columns" />
    from `test_product`
    where id =  #{id}
  </select>
  
  <insert id="insert">
    insert into `test_product` (
      `id`         ,
      `name`       ,
      `description`,
      `price`      ,
      `price_str`  ,
      `amount`     ,
      `create_by`  ,
      `create_date`,
      `update_by`  ,
      `update_date`,
      `status`     ,
      `status_name`
    ) values (
      #{id}         ,
      #{name}       ,
      #{description},
      #{price}      ,
      #{priceStr}   ,
      #{amount}     ,
      #{createBy}   ,
      #{createDate} ,
      #{updateBy}   ,
      #{updateDate} ,
      #{status}     ,
      #{statusName} 
    )
  </insert>
  
  <insert id="batchInsert">
    insert into `test_product` (
      `id`         ,
      `name`       ,
      `description`,
      `price`      ,
      `price_str`  ,
      `amount`     ,
      `create_by`  ,
      `create_date`,
      `update_by`  ,
      `update_date`,
      `status`     ,
      `status_name`
    ) 
     <foreach collection="list" item="obj" separator="union all">
            select
        #{obj.id}         ,
        #{obj.name}       ,
        #{obj.description},
        #{obj.price}      ,
        #{obj.priceStr}   ,
        #{obj.amount}     ,
        #{obj.createBy}   ,
        #{obj.createDate} ,
        #{obj.updateBy}   ,
        #{obj.updateDate} ,
        #{obj.status}     ,
        #{obj.statusName} 
         </foreach>
  </insert>
  
  <update id="update">
    update `test_product`
    <set>
      <if test="name != null"> 
        `name`        = #{name}       ,
      </if>
      <if test="description != null"> 
        `description` = #{description},
      </if>
      <if test="price != null"> 
        `price`       = #{price}      ,
      </if>
      <if test="priceStr != null"> 
        `price_str`   = #{priceStr}   ,
      </if>
      <if test="amount != null"> 
        `amount`      = #{amount}     ,
      </if>
      <if test="createBy != null"> 
        `create_by`   = #{createBy}   ,
      </if>
      <if test="createDate != null"> 
        `create_date` = #{createDate} ,
      </if>
      <if test="updateBy != null"> 
        `update_by`   = #{updateBy}   ,
      </if>
      <if test="updateDate != null"> 
        `update_date` = #{updateDate} ,
      </if>
      <if test="status != null"> 
        `status`      = #{status}     ,
      </if>
      <if test="statusName != null"> 
        `status_name` = #{statusName} ,
      </if>
    </set>
    where id =  #{id}
  </update>
  
  <delete id="delete">
    delete from `test_product` where id =  #{id}
  </delete>
  
  <delete id="deleteByIds">
    delete from `test_product`
    <where>
          <foreach collection="ids" item="id" separator="or">
              id = #{id}
          </foreach>
      </where>
  </delete>
  
  <select id="findList" resultType="com.github.hualuomoli.extend.base.entity.BaseProduct">
    select
      <include refid="columns" />
    from `test_product`
    <where>
      <include refid="querys" />
    </where>
  </select>
  
</mapper>